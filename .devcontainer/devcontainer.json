// See https://containers.dev/implementors/json_reference/ for configuration reference
{
	"name": "obs-widgets",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"remoteUser": "node",
	"postStartCommand": "yarn install --silent && yarn --version"
}